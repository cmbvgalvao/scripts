#!/bin/bash
#
# gitPush--To be run in a git repository. Receives a commit message, 
# adds, commits and pushes to the repository


echo "Enter commit message: "
read message

git add . && git commit -m "$message" && git push
